/**
 * Settings feature (Administration). Grouped system settings — view, edit, save, reload — built on
 * the shared layout/feedback/UI infrastructure and the `/admin/settings` backend contract.
 */
export { SettingsPage } from './settings-page';
export * from './types';
