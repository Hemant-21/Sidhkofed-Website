/**
 * Event News feature (Phase 15.3). Admin frontend for the derived News records — list, detail,
 * edit, and lifecycle. Creation happens via the event's publish-as-news action (events feature).
 */
export { NewsListPage } from './news-list-page';
export { NewsDetailPage } from './news-detail-page';
export { NewsEditPage } from './news-edit-page';
export { NEWS_RESOURCE } from './api';
export * from './types';
