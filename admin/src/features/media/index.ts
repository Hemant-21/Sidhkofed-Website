/**
 * Media Library feature (Phase 15.4). One reusable, fully API-driven library for images and
 * documents — grid/list browse, upload (single/bulk), preview with metadata edit, replace, usage
 * tracking, and archive/restore — built on the shared infrastructure and the backend media
 * endpoints (API spec §6/§7). Linked media cannot be hard-deleted (codex §5.1).
 */
export { MediaLibraryPage } from './media-library-page';
export { MEDIA_RESOURCE } from './api';
export * from './types';
