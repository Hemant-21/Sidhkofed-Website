/** `/dashboard-data` — dataset & metric management for dashboard reports. */
import { DashboardDataPage } from '@/features/dashboard-data';

export default function DashboardDataRoute() {
  return <DashboardDataPage />;
}
