/**
 * `/events` — Events & News list (Phase 15.3). Mounts the feature list page inside the admin shell.
 */
import { EventListPage } from '@/features/events';

export default function EventsRoute() {
  return <EventListPage />;
}
