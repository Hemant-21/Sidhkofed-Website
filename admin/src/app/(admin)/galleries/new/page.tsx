/**
 * `/galleries/new` — create a gallery (Phase 15.7 remediation).
 */
import { GalleryFormPage } from '@/features/galleries';

export default function NewGalleryRoute() {
  return <GalleryFormPage />;
}
