/**
 * `/dashboard/reports/new` — create a dashboard report definition (Phase 15.8).
 */
import { ReportFormPage } from '@/features/dashboard-data';

export default function NewDashboardReportRoute() {
  return <ReportFormPage />;
}
