/**
 * `/dashboard/reports` — Dashboard Reports list (Phase 15.8).
 */
import { ReportListPage } from '@/features/dashboard-data';

export default function DashboardReportsRoute() {
  return <ReportListPage />;
}
