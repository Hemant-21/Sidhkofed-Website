/**
 * `/programmes` — Programmes & Schemes list (Phase 15.5).
 */
import { ProgrammeListPage } from '@/features/programmes';

export default function ProgrammesRoute() {
  return <ProgrammeListPage />;
}
