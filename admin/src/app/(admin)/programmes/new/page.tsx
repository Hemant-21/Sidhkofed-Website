/**
 * `/programmes/new` — create a programme (Phase 15.5). Permission-gated inside the feature page.
 */
import { ProgrammeFormPage } from '@/features/programmes';

export default function NewProgrammeRoute() {
  return <ProgrammeFormPage />;
}
