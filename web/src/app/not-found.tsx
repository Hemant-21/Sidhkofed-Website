import { Container } from '@/components/ui/container';
import { NotFoundBlock } from '@/components/feedback/states';

export default function NotFound() {
  return (
    <Container className="py-16">
      <NotFoundBlock />
    </Container>
  );
}
